import { CommonServicePipe } from './common-service.pipe';

describe('CommonServicePipe', () => {
  it('create an instance', () => {
    const pipe = new CommonServicePipe();
    expect(pipe).toBeTruthy();
  });
});
