import { RoomPreferencePipe } from './room-preference.pipe';

describe('RoomPreferencePipe', () => {
  it('create an instance', () => {
    const pipe = new RoomPreferencePipe();
    expect(pipe).toBeTruthy();
  });
});
