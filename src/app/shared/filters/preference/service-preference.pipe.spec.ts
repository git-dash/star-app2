import { ServicePreferencePipe } from './service-preference.pipe';

describe('ServicePreferencePipe', () => {
  it('create an instance', () => {
    const pipe = new ServicePreferencePipe();
    expect(pipe).toBeTruthy();
  });
});
